<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Chat App</title>
  <script defer src="http://localhost:3000/socket.io/socket.io.js"></script>
  <script defer src="script.js"></script>

  <style>
    .bg-container {
      background-image: linear-gradient(to right, #ffffff, #f5f5f5, #e0e0e0, #d3d3d3);
      background-size: cover;
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 20px;
    }

    #room-container {
      background-color: #f1f1f1;
      display: flex;
      flex-direction: column; /* Stack rooms vertically for better readability */
      justify-content: center;
      margin: 20px;
      padding: 20px;
      border-radius: 5px;
    }

    .room-info {
      display: flex;
      flex-direction: row;
      align-items: center;
      margin: 10px 0;
    }

    .join {
      text-decoration: none;
      color: black;
      padding: 5px 10px;
      background-color: white;
      border: 1px solid #4a27a2;
      border-radius: 5px;
      font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
    }

    .roomINfo {
      background-image: radial-gradient(circle farthest-corner at 10% 20%, rgb(12, 159, 167) 0%, rgba(12, 116, 117, 1) 90%);
      width: 400px; /* Reduced width for better fit */
      border-radius: 5px;
      padding: 20px;
      display: flex;
      flex-direction: row;
      justify-content: center;
      text-align: center;
      color: white;
      font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
      margin-top: 20px;
    }

    .roomINfo input {
      width: 60%;
      border-radius: 5px;
      height: 30px;
      border: none;
      color: #4a27a2;
      padding: 0 10px;
    }

    .roomINfo button {
      width: 120px;
      background-color: white;
      color: #4a27a2;
      border-radius: 5px;
      height: 30px;
      border: none;
      margin-left: 5px;
    }
  </style>
</head>
<body>
  <div class="bg-container">
    <div id="room-container">
      <% Object.keys(rooms).forEach(room => { %>
        <div class="room-info">
          <p><%= room %></p>
          <a href="/<%= room %>" class="join">Join</a>
        </div>
      <% }) %>
    </div>
    <h1 style="color: #4a27a2;">Room Creation</h1>
    <form action="/room" method="POST" class="roomINfo">
      <input name="room" type="text" placeholder="enter room name to create new room" required>
      <button type="submit">Create Room</button>
    </form>
  </div>
</body>
</html>